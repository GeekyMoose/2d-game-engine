#include "gamemap/MapArea.h"

MapArea::MapArea(const int x, const int y)
    : m_coordinateX(x),
      m_coordinateY(y) {
}

bool MapArea::loadFromFile(const char* file) {
    // TODO
    return false;
}
